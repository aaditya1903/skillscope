# Normal forms

A short reference bundled with the demonstration skill.

- First: every column holds a single value.
- Second: no non-key column depends on part of a composite key.
- Third: no non-key column depends on another non-key column.
